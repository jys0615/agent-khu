apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: agent-khu
spec:
  clusterIP: None          # Headless — StatefulSet DNS 해석용
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
